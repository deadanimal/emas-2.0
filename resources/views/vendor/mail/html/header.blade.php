<tr>
    <td class="header">
        <a href="{{ $url }}" style="display: inline-block;">
            @if (trim($slot) === 'RMK')
                <img src="/img/logo.png" alt="logo">
            @else
                <img src="/img/logo.png" alt="logo">
            @endif
        </a>
    </td>
</tr>
